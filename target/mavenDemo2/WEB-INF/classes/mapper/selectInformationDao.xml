<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="cn.yy.dao.user.selectInformationDao">
    <select id="selectInformation" resultType="cn.yy.entity.student" parameterType="String">
        SELECT * FROM student WHERE student_id =#{studentId}

    </select>
</mapper>